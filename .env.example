# Groq API key — get one at https://console.groq.com
# For local dev, copy this to .env and fill in your key.
# In production (Vercel), set GROQ_API_KEY in the project's environment variables.
GROQ_API_KEY=
